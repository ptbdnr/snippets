
# set username, for all repo use option `--global`
git config user.name "Mona Lisa"
# read
git config user.name

# set user email, for all repo use option `--global`
git config user.email "email@example.com"
# read
git config user.email
